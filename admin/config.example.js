// Copy this file to `config.js` (gitignored) and fill in.
// These are the PUBLIC values from Supabase → Settings → API:
//   - Project URL
//   - anon / public key   (NOT the service_role key — that stays in the Edge Function)
window.YONA_CONFIG = {
  url: "https://YOUR-PROJECT-REF.supabase.co",
  anonKey: "your-anon-public-key",
};
