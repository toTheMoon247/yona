# Xcode / Swift
build/
DerivedData/
*.xcuserstate
*.xcscmblueprint
xcuserdata/
*.moved-aside
.swiftpm/
.build/

# macOS
.DS_Store

# Secrets / local config — never commit Supabase keys, env files
# App config lives in the git-ignored Supabase.plist (Supabase + Brandfetch).
*.env
.env*
Supabase.plist
*.local.*
